# Database Configuration (Disabled/Stubbed for Phase 3)
# spring.datasource.url=jdbc:postgresql://localhost:5432/kitchen_validator
# spring.datasource.username=postgres
# spring.datasource.password=password123

# Server Configuration
server.port=8080

# Increase Multipart Upload Limits for Kitchen Plans (e.g. 50MB)
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=50MB
spring.servlet.multipart.enabled=true

# Logging
logging.level.org.springframework.web=INFO
logging.level.com.factory.kitchen=DEBUG